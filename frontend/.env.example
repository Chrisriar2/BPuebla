# BASTA PUEBLA — frontend. Variable de BUILD (se inyecta en los HTML al compilar).
# En Vercel: Settings → Environment Variables → BP_API_URL.
# En local puedes omitirla: las apps usan http://localhost:3000 por defecto.

# URL pública del backend en Render (sin / final)
BP_API_URL=https://basta-puebla-api.onrender.com
